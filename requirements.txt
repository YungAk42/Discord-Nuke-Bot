First install python

You will need a coding software like notepad++ or vscode.

Type these commands into command promt.

pip install nextcord
pip install random
pip install nextcord
pip install datetime
pip install humanfriendly
pip install asyncio
pip install itertools

Read Readme.md
